@using DBFactory
@{
    ViewBag.Title = "List";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Unique Id
                    </th>
                    <th>
                        Navn
                    </th>

                    <th>
                        Handling
                    </th>
                </tr>
            </thead>

            <tbody>
                @foreach (var group in DBFactory.DB.Instance.GetGroups(int.MaxValue))
                {
                    <tr>
                        <td>
                            @group.Id
                        </td>
                        <td>

                            @*@Html.ActionLink(@group.Name, "ViewGroup", "Recipe", new { id = group.Id })*@
                            <a href="/Recipe/ViewGroup/@group.Id">@group.Name</a>
                            
                        </td>
                        <td>
                            @Html.ActionLink("Slett", "Delete", new { id = group.Id }, null)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>
